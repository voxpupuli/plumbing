language: bash
sudo: required
install:
  - ./build/install.sh
script:
  - ./build/build.sh
notifications:
  email: false
  irc:
    on_success: always
    on_failure: always
    channels:
      - "chat.freenode.org#voxpupuli-notifications"
